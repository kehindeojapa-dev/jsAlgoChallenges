/*
Return the next number from the integer passed

    Create a function that takes a number as an argument, increments the number by +1 and returns the result. Examples: addition(0) ->1, addition(9) ->10.
*/

function addition(int) {
    // int++;

    // or
    // int = int + 1;
    int +=1;
    return int;
}

addition();